[functions.healthkit-ingest]
verify_jwt = false
# Auth is handled inside the function with Supabase auth.getUser(). Gateway JWT
# verification is disabled only so the function can return its own clear 401 error.
